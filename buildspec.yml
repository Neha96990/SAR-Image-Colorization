version: 0.2

phases:
  install:
    runtime-versions:
      python: 3.11

  build:
    commands:
      - echo "Running tests..."
      - python -m unittest test_app.py

artifacts:
  files:
    - '**/*'
